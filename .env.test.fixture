# Canonical test fixture for vps-control-mcp. Copied to .env.test at test time
# by scripts/prepare-test-env.mjs. Do NOT add real secrets here — this file
# ships with the public tarball. Fixture paths need not exist on disk.
APP_DIR=/root/myapp
ALLOWED_PROCESSES=my-api,vps-mcp
